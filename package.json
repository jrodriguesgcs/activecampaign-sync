{
  "name": "activecampaign-vercel-sync",
  "version": "1.0.0",
  "description": "Sync ActiveCampaign contacts and deals to Vercel Postgres",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test-sync": "node scripts/test-sync.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "next": "^14.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
